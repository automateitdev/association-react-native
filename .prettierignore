# Not ours to format.
node_modules
.expo
dist
web-build
package-lock.json

# Generated by uniwind on every dev-server start, which undoes any formatting
# applied to it - it says so in its own first line. Formatting it once only
# means `format:check` fails again the next time the server runs.
src/uniwind.d.ts
